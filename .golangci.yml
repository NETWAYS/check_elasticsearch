run:
  deadline: 1m
  tests: false

linters:
  disable-all: false
  enable:
    - dogsled
    - dupl
    - whitespace
    - wsl
    - exportloopref
  presets:
    - bugs
    - unused
  fast: false
  disable:
    - gosec
    - funlen
    - scopelint
