run:
  timeout: 5m
  tests: false

issues.skip-files:
    - 'internal/config/http_config.go'
    - 'internal/config/config.go'

linters:
   enable-all: true
   disable:
     - execinquery
     - exportloopref
     - cyclop
     - depguard
     - dupl
     - exhaustruct
     - forbidigo
     - forcetypeassert
     - gci
     - gochecknoglobals
     - gochecknoinits
     - godox
     - err113
     - gofumpt
     - gomnd
     - mnd
     - lll
     - musttag
     - nakedret
     - nlreturn
     - nolintlint
     - nonamedreturns
     - tagliatelle
     - varnamelen
     - wrapcheck
linters-settings:
   estif:
     min-complexity: 4
   maligned:
     suggest-new: true
